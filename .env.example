# Prime Intellect Inference API (OpenAI-compatible). Enables GLM 5.2 without local GPU weights.
PRIME_API_KEY=your_prime_api_key
# PRIME_TEAM_ID=your_team_id

# GLM backend: prime (API) | hf (local HuggingFace weights) | heuristic (offline stand-in)
BURN_GLM_BACKEND=prime
# Prime Inference GLM models: z-ai/glm-5.2, z-ai/glm-5.1, z-ai/glm-5, z-ai/glm-4.7-flash,
# z-ai/glm-4.7, z-ai/glm-4.6, z-ai/glm-4.5-air, z-ai/glm-4.5
BURN_GLM_MODEL=z-ai/glm-5.2
BURN_GLM_MAX_TOKENS=8192

# Local HF on Prime GPU pod (LoRA TTT). Use -hf weights (NOT glm-4-9b-chat — breaks on transformers 5.x):
# BURN_GLM_BACKEND=hf
# BURN_GLM_MODEL=THUDM/glm-4-9b-chat-hf

# Reward shaping for the RL/TTT loop. One of:
#   v2_balanced (default) | throughput_first | efficiency_first | accuracy_strict | legacy
# See ttt/reward_variants.py. The reward-variant sweep (scripts/19_reward_sweep.py)
# overrides this per run.
BURN_REWARD_VARIANT=v2_balanced
# GRPO KL-to-reference coefficient (anchors test-time LoRA to the base model).
BURN_TTT_GRPO_KL=0.02

# Reward-variant sweep controls (scripts/19 + prime_bootstrap.sh):
#   REWARD_SWEEP=1 runs the sweep on a pod; REWARD_VARIANTS limits the set;
#   SWEEP_LOOP=0 iterates forever, focusing compute on SWEEP_TOP_K best rewards.
# REWARD_SWEEP=1
# REWARD_VARIANTS=v2_balanced,throughput_first,efficiency_first,accuracy_strict
# SWEEP_LOOP=1
# SWEEP_TOP_K=2
# SWEEP_ESCALATE=2

# Weights & Biases (optional; set WANDB_MODE=disabled to turn off)
WANDB_API_KEY=your_wandb_key
WANDB_PROJECT=burnttt
WANDB_RUN_NAME=reward-sweep

# Prime pod runner (scripts/17_prime_run_ttt.sh) — see .env.pod.example
# KEEP_POD=1
